# Allows making changes on a Live running vm!
https://libguestfs.org/guestfsd.8.html

quote: "f the guest is running the guestfsd daemon
specify live access.  In most libguestfs tools these options are --ro or
--live respectively."
